name: CI

on:
  pull_request: {}
  push:
    branches:
      - master

permissions:
  contents: read

concurrency:
  group: ci-interactive-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  source-evidence:
    name: Source checkout / evidence contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      head_checkout_sha: ${{ steps.head-evidence.outputs.actual_checkout_sha }}
      head_tree_sha: ${{ steps.head-evidence.outputs.actual_checkout_tree }}
    steps:
      - name: Checkout exact PR source or pushed commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Record source checkout evidence
        id: head-evidence
        env:
          CI_LANE: ${{ github.event_name == 'pull_request' && 'head_validation' || 'push' }}
          EVENT_NAME: ${{ github.event_name }}
          REPOSITORY: ${{ github.repository }}
          EVENT_SHA: ${{ github.sha }}
          EVENT_BEFORE: ${{ github.event.before }}
          SELECTED_REF: ${{ github.ref }}
          PR_BASE: ${{ github.event.pull_request.base.sha }}
          PR_HEAD: ${{ github.event.pull_request.head.sha }}
          PR_HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: node scripts/ciEvidence.mjs
      - name: Validate source-tree governance contracts
        run: npm run test:governance
      - name: Upload source checkout evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ci-source-evidence
          path: .ci-evidence
          include-hidden-files: true
          if-no-files-found: ignore

  change-scope:
    name: Change scope / routing contract
    needs: source-evidence
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      docs_only: ${{ steps.classify.outputs.docs_only }}
      frontend_changed: ${{ steps.classify.outputs.frontend_changed }}
      rust_changed: ${{ steps.classify.outputs.rust_changed }}
      macos_sensitive: ${{ steps.classify.outputs.macos_sensitive }}
      performance_sensitive: ${{ steps.classify.outputs.performance_sensitive }}
      high_risk: ${{ steps.classify.outputs.high_risk }}
      package_sensitive: ${{ steps.classify.outputs.package_sensitive }}
      dependency_sensitive: ${{ steps.classify.outputs.dependency_sensitive }}
      release_sensitive: ${{ steps.classify.outputs.release_sensitive }}
      all_domains_100k: ${{ steps.classify.outputs.all_domains_100k }}
      workflow_changed: ${{ steps.classify.outputs.workflow_changed }}
      base_missing: ${{ steps.classify.outputs.base_missing }}
      full_validation: ${{ steps.classify.outputs.full_validation }}
      perf_search: ${{ steps.classify.outputs.perf_search }}
      perf_scan_schema: ${{ steps.classify.outputs.perf_scan_schema }}
      perf_library_content: ${{ steps.classify.outputs.perf_library_content }}
      perf_intelligence: ${{ steps.classify.outputs.perf_intelligence }}
      perf_workspace_foundation: ${{ steps.classify.outputs.perf_workspace_foundation }}
      perf_preview_platform: ${{ steps.classify.outputs.perf_preview_platform }}
      performance_any: ${{ steps.classify.outputs.performance_any }}
      diff_base: ${{ steps.classify.outputs.diff_base }}
      diff_head: ${{ steps.classify.outputs.diff_head }}
      integration_checkout_sha: ${{ steps.merge-evidence.outputs.actual_checkout_sha }}
      integration_tree_sha: ${{ steps.merge-evidence.outputs.actual_checkout_tree }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Require source checkout evidence
        env:
          SOURCE_EVIDENCE_RESULT: ${{ needs.source-evidence.result }}
        run: |
          set -euo pipefail
          test "$SOURCE_EVIDENCE_RESULT" = success
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Classify changed files
        id: classify
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE: ${{ github.event.pull_request.base.sha }}
          PR_HEAD: ${{ github.event.pull_request.head.sha }}
          PUSH_BASE: ${{ github.event.before }}
          EVENT_HEAD: ${{ github.sha }}
          DISPATCH_FULL: "false"
          PR_LABELS: ${{ join(github.event.pull_request.labels.*.name, ',') }}
        run: node scripts/classifyCiChanges.mjs
      - name: Record merge-integration checkout evidence
        id: merge-evidence
        env:
          CI_LANE: ${{ github.event_name == 'pull_request' && 'merge_integration' || 'push' }}
          EVENT_NAME: ${{ github.event_name }}
          REPOSITORY: ${{ github.repository }}
          EVENT_SHA: ${{ github.sha }}
          EVENT_BEFORE: ${{ github.event.before }}
          SELECTED_REF: ${{ github.ref }}
          PR_BASE: ${{ github.event.pull_request.base.sha }}
          PR_HEAD: ${{ github.event.pull_request.head.sha }}
          PR_HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: node scripts/ciEvidence.mjs
      - name: Validate project governance
        run: npm run test:governance
      - name: Upload merge-integration checkout evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ci-merge-integration-evidence
          path: .ci-evidence
          include-hidden-files: true
          if-no-files-found: ignore

  validation-plan:
    name: Validation lane plan
    needs: [source-evidence, change-scope]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      plan_valid: ${{ steps.plan.outputs.plan_valid }}
      tree_equivalent: ${{ steps.plan.outputs.tree_equivalent }}
      head_validation_required: ${{ steps.plan.outputs.head_validation_required }}
      validation_lanes: ${{ steps.plan.outputs.validation_lanes }}
      head_checkout_sha: ${{ steps.plan.outputs.head_checkout_sha }}
      head_tree_sha: ${{ steps.plan.outputs.head_tree_sha }}
      integration_checkout_sha: ${{ steps.plan.outputs.integration_checkout_sha }}
      integration_tree_sha: ${{ steps.plan.outputs.integration_tree_sha }}
    steps:
      - name: Checkout workflow helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Build validation lane plan
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_SHA: ${{ github.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          SOURCE_EVIDENCE_RESULT: ${{ needs.source-evidence.result }}
          CHANGE_SCOPE_RESULT: ${{ needs.change-scope.result }}
          HEAD_CHECKOUT_SHA: ${{ needs.source-evidence.outputs.head_checkout_sha }}
          HEAD_TREE_SHA: ${{ needs.source-evidence.outputs.head_tree_sha }}
          INTEGRATION_CHECKOUT_SHA: ${{ needs.change-scope.outputs.integration_checkout_sha }}
          INTEGRATION_TREE_SHA: ${{ needs.change-scope.outputs.integration_tree_sha }}
          VALIDATION_LANE: ${{ github.event_name == 'pull_request' && 'merge_integration' || 'push' }}
        run: node scripts/ciValidationPlan.mjs

  docs-only-lanes:
    name: Documentation-only validation (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.docs_only == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout validation lane
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Validate documentation-only change
        env:
          DOCS_DIFF_BASE: ${{ needs.change-scope.outputs.diff_base }}
          DOCS_DIFF_HEAD: ${{ needs.change-scope.outputs.diff_head }}
        run: |
          npm run test:docs
          git diff --check "$DOCS_DIFF_BASE" "$DOCS_DIFF_HEAD"

  docs-only:
    name: Documentation-only validation
    needs: [change-scope, validation-plan, docs-only-lanes]
    if: ${{ always() && needs.change-scope.outputs.docs_only == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout CI validation helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node for CI validation helper
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Require all documentation validation lanes
        env:
          EVENT_NAME: ${{ github.event_name }}
          PLAN_RESULT: ${{ needs.validation-plan.result }}
          PLAN_VALID: ${{ needs.validation-plan.outputs.plan_valid }}
          TREE_EQUIVALENT: ${{ needs.validation-plan.outputs.tree_equivalent }}
          HEAD_VALIDATION_REQUIRED: ${{ needs.validation-plan.outputs.head_validation_required }}
          VALIDATION_LANES: ${{ needs.validation-plan.outputs.validation_lanes }}
          LANE_JOB_RESULT: ${{ needs.docs-only-lanes.result }}
        run: node scripts/ciValidationPlan.mjs --aggregate

  frontend-quality:
    name: Frontend and format quality (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.frontend_changed == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/.tmp-tests/playwright-browsers
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Cache Playwright Chromium
        id: playwright-browser-cache
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .tmp-tests/playwright-browsers
          key: zen-canvas-playwright-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('package-lock.json') }}
      - name: Install Chromium system dependencies
        shell: bash
        run: |
          set -euo pipefail
          max_attempts=2
          per_attempt_timeout=7m
          backoff_seconds=10

          cleanup_apt_processes() {
            local pids
            if pids="$(pgrep -x apt-get)"; then
              echo "[playwright-deps] terminating orphaned apt-get pids=${pids}"
              for pid in $pids; do
                if sudo -n kill -TERM "$pid"; then
                  echo "[playwright-deps] sent TERM to apt-get pid=${pid}"
                else
                  echo "[playwright-deps] apt-get pid=${pid} already exited"
                fi
              done
              sleep 2
              if pids="$(pgrep -x apt-get)"; then
                for pid in $pids; do
                  if sudo -n kill -KILL "$pid"; then
                    echo "[playwright-deps] sent KILL to apt-get pid=${pid}"
                  else
                    echo "[playwright-deps] apt-get pid=${pid} already exited"
                  fi
                done
                sleep 2
              fi
            fi
          }

          for attempt in $(seq 1 "$max_attempts"); do
            echo "[playwright-deps] attempt=${attempt}/${max_attempts} timeout=${per_attempt_timeout}"
            if timeout --signal=TERM --kill-after=30s "$per_attempt_timeout" npx playwright install-deps chromium; then
              status=0
            else
              status=$?
            fi

            if [ "$status" -eq 0 ]; then
              echo "[playwright-deps] PASS attempt=${attempt}"
              exit 0
            fi

            if [ "$status" -eq 124 ] || [ "$status" -eq 137 ]; then
              echo "[playwright-deps] TIMEOUT attempt=${attempt} exit=${status}"
              cleanup_apt_processes
            else
              echo "[playwright-deps] FAIL attempt=${attempt} exit=${status}"
            fi

            if [ "$attempt" -eq "$max_attempts" ]; then
              echo "[playwright-deps] exhausted retries"
              exit "$status"
            fi

            echo "[playwright-deps] backoff=${backoff_seconds}s before retry"
            sleep "$backoff_seconds"
          done
      - name: Install Chromium browser
        if: steps.playwright-browser-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          max_attempts=2
          per_attempt_timeout=5m
          backoff_seconds=10

          for attempt in $(seq 1 "$max_attempts"); do
            echo "[playwright-browser] attempt=${attempt}/${max_attempts} timeout=${per_attempt_timeout}"
            if timeout --signal=TERM --kill-after=30s "$per_attempt_timeout" npx playwright install chromium; then
              status=0
            else
              status=$?
            fi

            if [ "$status" -eq 0 ]; then
              echo "[playwright-browser] PASS attempt=${attempt}"
              exit 0
            fi

            if [ "$status" -eq 124 ] || [ "$status" -eq 137 ]; then
              echo "[playwright-browser] TIMEOUT attempt=${attempt} exit=${status}"
            else
              echo "[playwright-browser] FAIL attempt=${attempt} exit=${status}"
            fi

            if [ "$attempt" -eq "$max_attempts" ]; then
              echo "[playwright-browser] exhausted retries"
              exit "$status"
            fi

            echo "[playwright-browser] backoff=${backoff_seconds}s before retry"
            sleep "$backoff_seconds"
          done
      - name: Frontend tests and architecture checks
        run: |
          npm run typecheck
          npm test
          npm run test:remediation
          npm run test:performance:architecture
      - name: Build frontend
        run: npm run build:frontend
      - name: W2-01 real browser regression gate
        env:
          W201_SOURCE_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
          W201_EXPECTED_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          W201_LANE: ${{ matrix.validation_lane }}
        run: npm run test:browser:w2-01:real
      - name: Upload W2-01 browser gate failure artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: w2-01-browser-gate-failure-${{ matrix.validation_lane }}
          path: .tmp-tests/w2-01-browser-gate
          if-no-files-found: ignore
      - name: W2-10 interaction accessibility responsive browser gate
        env:
          W210_SOURCE_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
          W210_EXPECTED_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          W210_LANE: ${{ matrix.validation_lane }}
        run: npm run test:browser:w2-10:real
      - name: Upload W2-10 browser gate failure artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: w2-10-browser-gate-failure-${{ matrix.validation_lane }}
          path: .tmp-tests/w2-10-browser-gate
          if-no-files-found: ignore
      - name: W2-11 integrated experience performance browser gate
        env:
          W211_SOURCE_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
          W211_EXPECTED_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          W211_LANE: ${{ matrix.validation_lane }}
        run: npm run test:browser:w2-11:real
      - name: Upload W2-11 browser gate failure artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: w2-11-browser-gate-failure-${{ matrix.validation_lane }}
          path: .tmp-tests/w2-11-browser-gate
          if-no-files-found: ignore

  rust-windows:
    name: Rust quality (windows-latest) (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.rust_changed == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-Windows-v2-${{ matrix.validation_lane }}
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Rust format
        run: cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
      - name: Rust tests
        run: cargo test --manifest-path src-tauri/Cargo.toml --features desktop-runtime
      - name: Rust clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --features desktop-runtime --all-targets -- -D warnings
      - name: Native filesystem hardening smoke
        run: cargo test --manifest-path src-tauri/Cargo.toml --features "desktop-runtime native-qa" --test native_file_hardening_smoke -- --ignored --nocapture --test-threads=1

  rust-macos:
    name: Rust quality (macos-latest) (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.macos_sensitive == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: macos-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Verify Apple Silicon runner
        shell: bash
        run: |
          test "$(uname -m)" = "arm64"
          sw_vers
          rustc -vV
          test "$(rustc -vV | sed -n 's/^host: //p')" = "aarch64-apple-darwin"
          echo "MACOSX_DEPLOYMENT_TARGET=13.0" >> "$GITHUB_ENV"
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-macOS-v2-${{ matrix.validation_lane }}
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Rust format
        run: cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
      - name: Rust tests
        run: |
          cargo test --manifest-path src-tauri/Cargo.toml --features "desktop-runtime native-qa" -- \
            --skip macos_mutation_parity_supports_move_copy_replace_restore_and_delete \
            --skip macos_symlink_and_package_mutations_keep_namespace_boundaries \
            --skip macos_target_conflict_preserves_both_objects_without_claim_artifacts \
            --skip macos_target_creation_race_never_loses_source_payload \
            --skip macos_expanded_adversarial_attack_matrix_reports_zero_wrong_commit_or_loss \
            --skip macos_cross_volume_source_mutation_is_rejected_when_real_fixture_is_provided
      - name: Rust clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --features "desktop-runtime native-qa" --all-targets -- -D warnings
      - name: macOS race validation (serial, once)
        env:
          ZEN_CANVAS_MACOS_RACE_ITERATIONS: "10000"
          ZEN_CANVAS_MACOS_EXPANDED_RACE_ITERATIONS: "10000"
        run: cargo test --manifest-path src-tauri/Cargo.toml --features "desktop-runtime native-qa" --test macos_mutation_fail_closed -- --nocapture --test-threads=1

  performance-prepare:
    name: Performance / Prepare (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.performance_any == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    outputs:
      binary_build_identity: ${{ steps.binary-build-identity.outputs.build_identity }}
      fixture_cache_key: ${{ steps.prepare-artifacts.outputs.fixture_cache_key }}
      fixture_identity: ${{ steps.prepare-artifacts.outputs.fixture_identity }}
      fixture_format_version: ${{ steps.prepare-artifacts.outputs.fixture_format_version }}
    runs-on: windows-latest
    timeout-minutes: 30
    env:
      PERF_SEARCH: ${{ needs.change-scope.outputs.perf_search }}
      PERF_SCAN_SCHEMA: ${{ needs.change-scope.outputs.perf_scan_schema }}
      PERF_LIBRARY_CONTENT: ${{ needs.change-scope.outputs.perf_library_content }}
      PERF_INTELLIGENCE: ${{ needs.change-scope.outputs.perf_intelligence }}
      PERF_WORKSPACE_FOUNDATION: ${{ needs.change-scope.outputs.perf_workspace_foundation }}
      PERF_PREVIEW_PLATFORM: ${{ needs.change-scope.outputs.perf_preview_platform }}
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
      CACHE_WRITE_ALLOWED: ${{ github.event_name != 'pull_request' || (github.event.pull_request.head.repo.full_name == github.repository && matrix.validation_lane == 'merge_integration') }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Compute performance binary build identity
        id: binary-build-identity
        shell: pwsh
        run: node scripts/performanceBuildIdentity.mjs "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}"
      - name: Restore reusable prepared binary cache
        id: binary-cache
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .performance-cache/binaries
          key: zen-canvas-perf-binaries-${{ runner.os }}-${{ runner.arch }}-${{ steps.binary-build-identity.outputs.build_identity }}
      - name: Rust cache
        if: ${{ steps.binary-cache.outputs.cache-hit != 'true' }}
        id: rust-cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-Windows-performance-v3-${{ matrix.validation_lane }}
          add-job-id-key: false
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Report Rust cache state
        if: ${{ steps.binary-cache.outputs.cache-hit != 'true' }}
        run: echo "[perf-prepare] rust-cache=${{ steps.rust-cache.outputs.cache-hit == 'true' && 'hit' || 'miss' }}"
      - name: Compute reusable fixture identity
        if: ${{ needs.change-scope.outputs.perf_library_content == 'true' }}
        id: fixture-identity
        shell: pwsh
        run: node scripts/performanceFixtureIdentity.mjs "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}"
      - name: Restore reusable fixture cache
        if: ${{ needs.change-scope.outputs.perf_library_content == 'true' }}
        id: fixture-cache
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .tmp-performance-fixtures/cache
          key: ${{ steps.fixture-identity.outputs.fixture_cache_key }}
      - name: Prepare performance binaries and fixtures
        id: prepare-artifacts
        shell: pwsh
        run: |
          node scripts/preparePerformanceBinaries.mjs "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--cache-root=.performance-cache/binaries" "--output=.performance-artifacts/binaries"
          if ("${{ needs.change-scope.outputs.perf_library_content }}" -eq "true") {
            node scripts/preparePerformanceFixtures.mjs "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--cache-root=.tmp-performance-fixtures/cache" "--fixture-identity=${{ steps.fixture-identity.outputs.fixture_identity }}"
          }
      - name: Save prepared binary cache
        if: ${{ steps.binary-cache.outputs.cache-hit != 'true' && env.CACHE_WRITE_ALLOWED == 'true' }}
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .performance-cache/binaries
          key: zen-canvas-perf-binaries-${{ runner.os }}-${{ runner.arch }}-${{ steps.binary-build-identity.outputs.build_identity }}
      - name: Save prepared fixture cache
        if: ${{ needs.change-scope.outputs.perf_library_content == 'true' && steps.fixture-cache.outputs.cache-hit != 'true' && env.CACHE_WRITE_ALLOWED == 'true' }}
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .tmp-performance-fixtures/cache
          key: ${{ steps.fixture-identity.outputs.fixture_cache_key }}
      - name: Upload Search prepared binaries
        if: ${{ needs.change-scope.outputs.perf_search == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: perf-bin-search-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries/search
          if-no-files-found: error
          retention-days: 2
      - name: Upload Scan and Schema prepared binaries
        if: ${{ needs.change-scope.outputs.perf_scan_schema == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: perf-bin-scan-schema-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries/scan-schema
          if-no-files-found: error
          retention-days: 2
      - name: Upload Library and Content prepared binaries
        if: ${{ needs.change-scope.outputs.perf_library_content == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: perf-bin-library-content-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries/library-content
          if-no-files-found: error
          retention-days: 2
      - name: Upload Intelligence prepared binaries
        if: ${{ needs.change-scope.outputs.perf_intelligence == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: perf-bin-intelligence-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries/intelligence
          if-no-files-found: error
          retention-days: 2
      - name: Upload Workspace Foundation prepared binaries
        if: ${{ needs.change-scope.outputs.perf_workspace_foundation == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: perf-bin-workspace-foundation-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries/workspace-foundation
          if-no-files-found: error
          retention-days: 2

      - name: Upload Preview Platform prepared binaries
        if: ${{ needs.change-scope.outputs.perf_preview_platform == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: perf-bin-preview-platform-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries/preview-platform
          if-no-files-found: error
          retention-days: 2

  performance-search:
    name: Performance / Search (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan, performance-prepare]
    if: ${{ needs.change-scope.outputs.perf_search == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Download Search prepared binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: perf-bin-search-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries
      - name: Read prepared Search binary identity
        id: binary-identity
        shell: pwsh
        run: |
          $manifest = Get-Content ".performance-artifacts/binaries/manifest.json" | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($manifest.buildIdentity)) { throw "Prepared Search binary manifest has no build identity." }
          "build_identity=$($manifest.buildIdentity)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      - name: Run Search performance suite from prepared binary
        shell: pwsh
        run: |
          $performanceTemp = Join-Path $PWD ".performance-temp"
          New-Item -ItemType Directory -Force -Path $performanceTemp | Out-Null
          $env:TEMP = $performanceTemp
          $env:TMP = $performanceTemp
          node scripts/runPerformanceSuite.mjs "--suite=search" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--build-identity=${{ steps.binary-identity.outputs.build_identity }}"

  performance-scan-schema:
    name: Performance / Scan & Schema (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan, performance-prepare]
    if: ${{ needs.change-scope.outputs.perf_scan_schema == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Download Scan and Schema prepared binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: perf-bin-scan-schema-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries
      - name: Read prepared Scan and Schema binary identity
        id: binary-identity
        shell: pwsh
        run: |
          $manifest = Get-Content ".performance-artifacts/binaries/manifest.json" | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($manifest.buildIdentity)) { throw "Prepared Scan and Schema binary manifest has no build identity." }
          "build_identity=$($manifest.buildIdentity)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      - name: Run Scan and Schema performance suite from prepared binary
        shell: pwsh
        run: |
          $performanceTemp = Join-Path $PWD ".performance-temp"
          New-Item -ItemType Directory -Force -Path $performanceTemp | Out-Null
          $env:TEMP = $performanceTemp
          $env:TMP = $performanceTemp
          node scripts/runPerformanceSuite.mjs "--suite=scan-schema" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--build-identity=${{ steps.binary-identity.outputs.build_identity }}"

  performance-library-content:
    name: Performance / Library & Content (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan, performance-prepare]
    if: ${{ needs.change-scope.outputs.perf_library_content == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Download Library and Content prepared binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: perf-bin-library-content-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries
      - name: Read prepared Library and Content identities
        id: binary-identity
        shell: pwsh
        run: |
          $manifest = Get-Content ".performance-artifacts/binaries/manifest.json" | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($manifest.buildIdentity)) { throw "Prepared Library and Content binary manifest has no build identity." }
          "build_identity=$($manifest.buildIdentity)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          node scripts/performanceFixtureIdentity.mjs "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}"
      - name: Restore exact Library and Content fixture cache
        id: fixture-cache
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .tmp-performance-fixtures/cache
          key: ${{ steps.binary-identity.outputs.fixture_cache_key }}
      - name: Require exact Library and Content fixture cache
        shell: pwsh
        run: |
          if ("${{ steps.fixture-cache.outputs.cache-hit }}" -ne "true") { throw "Exact prepared fixture cache was not restored." }
      - name: Run Library and Content performance suite from prepared binary
        shell: pwsh
        run: |
          $performanceTemp = Join-Path $PWD ".performance-temp"
          New-Item -ItemType Directory -Force -Path $performanceTemp | Out-Null
          $env:TEMP = $performanceTemp
          $env:TMP = $performanceTemp
          node scripts/runPerformanceSuite.mjs "--suite=library-content" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--build-identity=${{ steps.binary-identity.outputs.build_identity }}" "--fixture-root=.tmp-performance-fixtures/cache" "--fixture-identity=${{ steps.binary-identity.outputs.fixture_identity }}"

  performance-intelligence:
    name: Performance / Intelligence (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan, performance-prepare]
    if: ${{ needs.change-scope.outputs.perf_intelligence == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Download Intelligence prepared binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: perf-bin-intelligence-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries
      - name: Read prepared Intelligence binary identity
        id: binary-identity
        shell: pwsh
        run: |
          $manifest = Get-Content ".performance-artifacts/binaries/manifest.json" | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($manifest.buildIdentity)) { throw "Prepared Intelligence binary manifest has no build identity." }
          "build_identity=$($manifest.buildIdentity)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      - name: Run Intelligence performance suite from prepared binary
        shell: pwsh
        run: |
          $performanceTemp = Join-Path $PWD ".performance-temp"
          New-Item -ItemType Directory -Force -Path $performanceTemp | Out-Null
          $env:TEMP = $performanceTemp
          $env:TMP = $performanceTemp
          node scripts/runPerformanceSuite.mjs "--suite=intelligence" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--build-identity=${{ steps.binary-identity.outputs.build_identity }}"

  performance-workspace-foundation:
    name: Performance / Workspace Foundation (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan, performance-prepare]
    if: ${{ needs.change-scope.outputs.perf_workspace_foundation == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Download Workspace Foundation prepared binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: perf-bin-workspace-foundation-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries
      - name: Read prepared Workspace Foundation binary identity
        id: binary-identity
        shell: pwsh
        run: |
          $manifest = Get-Content ".performance-artifacts/binaries/manifest.json" | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($manifest.buildIdentity)) { throw "Prepared Workspace Foundation binary manifest has no build identity." }
          "build_identity=$($manifest.buildIdentity)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      - name: Run Workspace Foundation performance suite from prepared binary
        shell: pwsh
        run: |
          $performanceTemp = Join-Path $PWD ".performance-temp"
          New-Item -ItemType Directory -Force -Path $performanceTemp | Out-Null
          $env:TEMP = $performanceTemp
          $env:TMP = $performanceTemp
          node scripts/runPerformanceSuite.mjs "--suite=workspace-foundation" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--build-identity=${{ steps.binary-identity.outputs.build_identity }}"

  performance-preview-platform:
    name: Performance / Preview Platform (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan, performance-prepare]
    if: ${{ needs.change-scope.outputs.perf_preview_platform == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 25
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Download Preview Platform prepared binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: perf-bin-preview-platform-${{ matrix.validation_lane }}
          path: .performance-artifacts/binaries
      - name: Read prepared Preview Platform binary identity
        id: binary-identity
        shell: pwsh
        run: |
          $manifest = Get-Content ".performance-artifacts/binaries/manifest.json" | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($manifest.buildIdentity)) { throw "Prepared Preview Platform binary manifest has no build identity." }
          "build_identity=$($manifest.buildIdentity)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      - name: Run Preview Platform Phase A preparation suite from prepared binary
        shell: pwsh
        run: |
          $performanceTemp = Join-Path $PWD ".performance-temp"
          New-Item -ItemType Directory -Force -Path $performanceTemp | Out-Null
          $env:TEMP = $performanceTemp
          $env:TMP = $performanceTemp
          node scripts/runPerformanceSuite.mjs "--suite=preview-platform" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" "--prepared-binaries=.performance-artifacts/binaries" "--build-identity=${{ steps.binary-identity.outputs.build_identity }}"

  performance-profile:
    name: Performance profile
    needs: [change-scope, validation-plan, performance-prepare, performance-search, performance-scan-schema, performance-library-content, performance-intelligence, performance-workspace-foundation, performance-preview-platform]
    if: ${{ always() && needs.change-scope.outputs.docs_only != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout CI validation helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node for CI validation helper
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Verify validation plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          PLAN_RESULT: ${{ needs.validation-plan.result }}
          PLAN_VALID: ${{ needs.validation-plan.outputs.plan_valid }}
          TREE_EQUIVALENT: ${{ needs.validation-plan.outputs.tree_equivalent }}
          HEAD_VALIDATION_REQUIRED: ${{ needs.validation-plan.outputs.head_validation_required }}
          VALIDATION_LANES: ${{ needs.validation-plan.outputs.validation_lanes }}
        run: node scripts/ciValidationPlan.mjs --aggregate
      - name: Verify expected performance shard results
        env:
          CLASSIFIER: ${{ needs.change-scope.result }}
          VALIDATION_PLAN: ${{ needs.validation-plan.result }}
          PREPARE: ${{ needs.performance-prepare.result }}
          EXPECTED_ANY: ${{ needs.change-scope.outputs.performance_any }}
          EXPECTED_SEARCH: ${{ needs.change-scope.outputs.perf_search }}
          EXPECTED_SCAN_SCHEMA: ${{ needs.change-scope.outputs.perf_scan_schema }}
          EXPECTED_LIBRARY_CONTENT: ${{ needs.change-scope.outputs.perf_library_content }}
          EXPECTED_INTELLIGENCE: ${{ needs.change-scope.outputs.perf_intelligence }}
          EXPECTED_WORKSPACE_FOUNDATION: ${{ needs.change-scope.outputs.perf_workspace_foundation }}
          EXPECTED_PREVIEW_PLATFORM: ${{ needs.change-scope.outputs.perf_preview_platform }}
          ACTUAL_SEARCH: ${{ needs.performance-search.result }}
          ACTUAL_SCAN_SCHEMA: ${{ needs.performance-scan-schema.result }}
          ACTUAL_LIBRARY_CONTENT: ${{ needs.performance-library-content.result }}
          ACTUAL_INTELLIGENCE: ${{ needs.performance-intelligence.result }}
          ACTUAL_WORKSPACE_FOUNDATION: ${{ needs.performance-workspace-foundation.result }}
          ACTUAL_PREVIEW_PLATFORM: ${{ needs.performance-preview-platform.result }}
        run: |
          set -euo pipefail
          test "$CLASSIFIER" = success
          test "$VALIDATION_PLAN" = success
          if [ "$EXPECTED_ANY" = true ]; then
            test "$PREPARE" = success
          fi
          check_shard() {
            expected="$1"
            actual="$2"
            label="$3"
            if [ "$expected" = true ]; then
              test "$actual" = success || { echo "$label expected success, got $actual"; exit 1; }
            else
              test "$actual" = skipped || { echo "$label expected skipped, got $actual"; exit 1; }
            fi
          }
          check_shard "$EXPECTED_SEARCH" "$ACTUAL_SEARCH" search
          check_shard "$EXPECTED_SCAN_SCHEMA" "$ACTUAL_SCAN_SCHEMA" scan-schema
          check_shard "$EXPECTED_LIBRARY_CONTENT" "$ACTUAL_LIBRARY_CONTENT" library-content
          check_shard "$EXPECTED_INTELLIGENCE" "$ACTUAL_INTELLIGENCE" intelligence
          check_shard "$EXPECTED_WORKSPACE_FOUNDATION" "$ACTUAL_WORKSPACE_FOUNDATION" workspace-foundation
          check_shard "$EXPECTED_PREVIEW_PLATFORM" "$ACTUAL_PREVIEW_PLATFORM" preview-platform
          {
            echo "## Performance profile"
            echo "- Search: $ACTUAL_SEARCH (expected=$EXPECTED_SEARCH)"
            echo "- Scan/Schema: $ACTUAL_SCAN_SCHEMA (expected=$EXPECTED_SCAN_SCHEMA)"
            echo "- Library/Content: $ACTUAL_LIBRARY_CONTENT (expected=$EXPECTED_LIBRARY_CONTENT)"
            echo "- Intelligence: $ACTUAL_INTELLIGENCE (expected=$EXPECTED_INTELLIGENCE)"
            echo "- Workspace Foundation: $ACTUAL_WORKSPACE_FOUNDATION (expected=$EXPECTED_WORKSPACE_FOUNDATION)"
            echo "- Preview Platform: $ACTUAL_PREVIEW_PLATFORM (expected=$EXPECTED_PREVIEW_PLATFORM)"
          } >> "$GITHUB_STEP_SUMMARY"

  performance-macos:
    name: Native macOS performance (arm64) (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.docs_only != 'true' && (needs.change-scope.outputs.full_validation == 'true' || needs.change-scope.outputs.performance_sensitive == 'true') }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: macos-latest
    timeout-minutes: 35
    env:
      PERF_CHECKOUT_SHA: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
      CACHE_WRITE_ALLOWED: ${{ github.event_name != 'pull_request' || (github.event.pull_request.head.repo.full_name == github.repository && matrix.validation_lane == 'merge_integration') }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Verify Apple Silicon runner
        shell: bash
        run: |
          test "$(uname -m)" = "arm64"
          rustc -vV
          test "$(rustc -vV | sed -n 's/^host: //p')" = "aarch64-apple-darwin"
          echo "MACOSX_DEPLOYMENT_TARGET=13.0" >> "$GITHUB_ENV"
      - name: Setup Node for the shared performance framework
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-macOS-native-performance-${{ matrix.validation_lane }}
          cache-on-failure: true
      - name: Compute native prepared binary identity
        id: native-binary-build-identity
        shell: bash
        run: node scripts/performanceBuildIdentity.mjs "--suites=workspace-foundation,preview-platform" "--profile=${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}"
      - name: Restore native prepared binary cache
        id: native-binary-cache
        uses: actions/cache/restore@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .performance-cache/binaries
          key: zen-canvas-perf-binaries-${{ runner.os }}-${{ runner.arch }}-${{ steps.native-binary-build-identity.outputs.build_identity }}
      - name: Report native prepared binary cache state
        shell: bash
        run: echo "[perf-native-macos] prepared-binary-cache=${{ steps.native-binary-cache.outputs.cache-hit == 'true' && 'hit' || 'miss' }}"
      - name: Run native macOS corpus and bookkeeping profiles
        shell: bash
        env:
          FULL_VALIDATION: ${{ needs.change-scope.outputs.full_validation }}
        run: |
          if [ "$FULL_VALIDATION" = true ]; then
            export ZC_MACOS_NATIVE_FULL_PROFILE=1
          fi
          cargo test --manifest-path src-tauri/Cargo.toml --features desktop-runtime --lib native_classifier_real_mixed_filesystem_corpus_is_atomic -- --nocapture
          cargo test --manifest-path src-tauri/Cargo.toml --features desktop-runtime --lib macos_native_bookkeeping_benchmark_is_bounded_by_unique_identity -- --nocapture
          cargo test --manifest-path src-tauri/Cargo.toml --features desktop-runtime --test macos_file_provider_feasibility -- --nocapture
      - name: Prepare native Workspace Foundation performance binary
        shell: bash
        run: |
          node scripts/preparePerformanceBinaries.mjs \
            --suites=workspace-foundation,preview-platform \
            --profile="${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" \
            --cache-root=.performance-cache/binaries \
            --output=.performance-artifacts/binaries
      - name: Save native prepared binary cache
        if: ${{ steps.native-binary-cache.outputs.cache-hit != 'true' && env.CACHE_WRITE_ALLOWED == 'true' }}
        uses: actions/cache/save@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .performance-cache/binaries
          key: zen-canvas-perf-binaries-${{ runner.os }}-${{ runner.arch }}-${{ steps.native-binary-build-identity.outputs.build_identity }}
      - name: Run File Workspace Foundation through the shared performance framework
        shell: bash
        run: |
          export PERFORMANCE_TEMP="$PWD/.performance-temp"
          mkdir -p "$PERFORMANCE_TEMP"
          export TEMP="$PERFORMANCE_TEMP"
          export TMP="$PERFORMANCE_TEMP"
          node scripts/runPerformanceSuite.mjs \
            --suite=workspace-foundation \
            --profile="${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" \
            --prepared-binaries=.performance-artifacts/binaries/workspace-foundation
      - name: Run Preview Platform Phase A preparation suite on native macOS
        if: ${{ needs.change-scope.outputs.full_validation == 'true' || needs.change-scope.outputs.perf_preview_platform == 'true' }}
        shell: bash
        run: |
          export PERFORMANCE_TEMP="$PWD/.performance-temp-preview"
          mkdir -p "$PERFORMANCE_TEMP"
          export TEMP="$PERFORMANCE_TEMP"
          export TMP="$PERFORMANCE_TEMP"
          node scripts/runPerformanceSuite.mjs \
            --suite=preview-platform \
            --profile="${{ needs.change-scope.outputs.full_validation == 'true' && 'full' || 'extended' }}" \
            --prepared-binaries=.performance-artifacts/binaries/preview-platform

  build-windows:
    name: Release compile (windows-latest) (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.release_sensitive == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-Windows-build-v2-${{ matrix.validation_lane }}
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Release compile without frontend rebuild
        run: npm run check:rust:release

  build-macos:
    name: Release compile (macos-latest) (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.release_sensitive == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: macos-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Verify Apple Silicon runner
        shell: bash
        run: |
          test "$(uname -m)" = "arm64"
          sw_vers
          rustc -vV
          test "$(rustc -vV | sed -n 's/^host: //p')" = "aarch64-apple-darwin"
          echo "MACOSX_DEPLOYMENT_TARGET=13.0" >> "$GITHUB_ENV"
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-macOS-build-v2-${{ matrix.validation_lane }}
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Release compile without frontend rebuild
        run: npm run check:rust:release

  package-windows:
    name: Package NSIS (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.full_validation == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-Windows-package-v2-${{ matrix.validation_lane }}
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Build NSIS package
        run: npm run build

  package-macos:
    name: Package unsigned DMG (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.full_validation == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Verify Apple Silicon runner
        shell: bash
        run: |
          test "$(uname -m)" = "arm64"
          sw_vers
          rustc -vV
          test "$(rustc -vV | sed -n 's/^host: //p')" = "aarch64-apple-darwin"
          echo "MACOSX_DEPLOYMENT_TARGET=13.0" >> "$GITHUB_ENV"
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Rust cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          workspaces: src-tauri -> target
          shared-key: zen-canvas-macOS-package-v2-${{ matrix.validation_lane }}
          cache-workspace-crates: true
          cache-targets: true
          cache-on-failure: true
      - name: Build unsigned DMG
        run: npm run build -- --no-sign

  package-smoke:
    name: Package metadata smoke (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.package_sensitive == 'true' && needs.change-scope.outputs.full_validation != 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Validate package metadata and bundle inputs
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import fs from "node:fs";
          import path from "node:path";
          const packageJson = JSON.parse(fs.readFileSync("package.json", "utf8"));
          const lockfile = JSON.parse(fs.readFileSync("package-lock.json", "utf8"));
          const tauri = JSON.parse(fs.readFileSync("src-tauri/tauri.conf.json", "utf8"));
          const rootLock = lockfile.packages?.[""];
          if (!rootLock || rootLock.name !== packageJson.name || rootLock.version !== packageJson.version) {
            throw new Error("package.json and package-lock.json root metadata must match");
          }
          const targets = tauri.bundle?.targets ?? [];
          for (const target of ["nsis", "dmg"]) {
            if (!targets.includes(target)) throw new Error(`bundle target ${target} is missing`);
          }
          for (const icon of tauri.bundle?.icon ?? []) {
            if (!fs.existsSync(path.resolve("src-tauri", icon))) throw new Error(`bundle icon is missing: ${icon}`);
          }
          console.log(`package smoke passed for ${packageJson.name}@${packageJson.version}`);
          NODE

  dependency-audit:
    name: Dependency audit (${{ matrix.validation_lane }})
    needs: [change-scope, validation-plan]
    if: ${{ needs.change-scope.outputs.dependency_sensitive == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        validation_lane: ${{ fromJSON(needs.validation-plan.outputs.validation_lanes) }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CARGO_AUDIT_VERSION: "0.22.2"
      CARGO_AUDIT_ROOT: ${{ github.workspace }}/.cargo-audit
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ matrix.validation_lane == 'head_validation' && github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: npm audit
        run: npm run security:audit
      - name: Record Rust toolchain identity
        id: rust-toolchain
        shell: bash
        run: echo "rustc_identity=$(rustc -Vv | sha256sum | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
      - name: Cache pinned cargo-audit
        id: cargo-audit-cache
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: .cargo-audit
          key: zen-canvas-cargo-audit-${{ runner.os }}-${{ runner.arch }}-${{ steps.rust-toolchain.outputs.rustc_identity }}-${{ env.CARGO_AUDIT_VERSION }}
      - name: Install pinned cargo-audit
        if: ${{ steps.cargo-audit-cache.outputs.cache-hit != 'true' }}
        shell: bash
        run: cargo install cargo-audit --version "$CARGO_AUDIT_VERSION" --locked --root "$CARGO_AUDIT_ROOT"
      - name: Verify pinned cargo-audit
        shell: bash
        run: |
          test -x "$CARGO_AUDIT_ROOT/bin/cargo-audit"
          "$CARGO_AUDIT_ROOT/bin/cargo-audit" --version | grep -F "$CARGO_AUDIT_VERSION"
          echo "$CARGO_AUDIT_ROOT/bin" >> "$GITHUB_PATH"
      - name: RustSec audit
        run: npm run security:audit:rust

  quality-windows:
    name: Quality (windows-latest)
    needs: [change-scope, validation-plan, frontend-quality, rust-windows, performance-profile, build-windows, package-windows, package-smoke, dependency-audit]
    if: ${{ always() && needs.change-scope.outputs.docs_only != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout CI validation helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node for CI validation helper
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Verify validation plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          PLAN_RESULT: ${{ needs.validation-plan.result }}
          PLAN_VALID: ${{ needs.validation-plan.outputs.plan_valid }}
          TREE_EQUIVALENT: ${{ needs.validation-plan.outputs.tree_equivalent }}
          HEAD_VALIDATION_REQUIRED: ${{ needs.validation-plan.outputs.head_validation_required }}
          VALIDATION_LANES: ${{ needs.validation-plan.outputs.validation_lanes }}
        run: node scripts/ciValidationPlan.mjs --aggregate
      - name: Verify Windows gate dependencies
        env:
          CLASSIFIER: ${{ needs.change-scope.result }}
          VALIDATION_PLAN: ${{ needs.validation-plan.result }}
          FRONTEND_EXPECTED: ${{ needs.change-scope.outputs.frontend_changed }}
          RUST_EXPECTED: ${{ needs.change-scope.outputs.rust_changed }}
          BUILD_EXPECTED: ${{ needs.change-scope.outputs.release_sensitive }}
          PACKAGE_EXPECTED: ${{ needs.change-scope.outputs.full_validation }}
          PACKAGE_SMOKE_EXPECTED: ${{ needs.change-scope.outputs.package_sensitive == 'true' && needs.change-scope.outputs.full_validation != 'true' }}
          AUDIT_EXPECTED: ${{ needs.change-scope.outputs.dependency_sensitive }}
          FRONTEND: ${{ needs.frontend-quality.result }}
          RUST: ${{ needs.rust-windows.result }}
          PERFORMANCE: ${{ needs.performance-profile.result }}
          BUILD: ${{ needs.build-windows.result }}
          PACKAGE: ${{ needs.package-windows.result }}
          PACKAGE_SMOKE: ${{ needs.package-smoke.result }}
          AUDIT: ${{ needs.dependency-audit.result }}
        run: |
          set -euo pipefail
          test "$CLASSIFIER" = success
          test "$VALIDATION_PLAN" = success
          check_expected() {
            expected="$1"
            actual="$2"
            label="$3"
            if [ "$expected" = true ]; then
              test "$actual" = success || { echo "$label expected success, got $actual"; exit 1; }
            else
              test "$actual" = skipped || { echo "$label expected skipped, got $actual"; exit 1; }
            fi
          }
          check_expected "$FRONTEND_EXPECTED" "$FRONTEND" frontend
          check_expected "$RUST_EXPECTED" "$RUST" rust-windows
          check_expected true "$PERFORMANCE" performance-profile
          check_expected "$BUILD_EXPECTED" "$BUILD" release-compile-windows
          check_expected "$PACKAGE_EXPECTED" "$PACKAGE" package-windows
          check_expected "$PACKAGE_SMOKE_EXPECTED" "$PACKAGE_SMOKE" package-smoke
          check_expected "$AUDIT_EXPECTED" "$AUDIT" dependency-audit

  quality-macos:
    name: Quality (macos-latest)
    needs: [change-scope, validation-plan, frontend-quality, rust-macos, performance-profile, performance-macos, build-macos, package-macos, package-smoke, dependency-audit]
    if: ${{ always() && needs.change-scope.outputs.docs_only != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout CI validation helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node for CI validation helper
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
      - name: Verify validation plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          PLAN_RESULT: ${{ needs.validation-plan.result }}
          PLAN_VALID: ${{ needs.validation-plan.outputs.plan_valid }}
          TREE_EQUIVALENT: ${{ needs.validation-plan.outputs.tree_equivalent }}
          HEAD_VALIDATION_REQUIRED: ${{ needs.validation-plan.outputs.head_validation_required }}
          VALIDATION_LANES: ${{ needs.validation-plan.outputs.validation_lanes }}
        run: node scripts/ciValidationPlan.mjs --aggregate
      - name: Verify macOS gate dependencies
        env:
          CLASSIFIER: ${{ needs.change-scope.result }}
          VALIDATION_PLAN: ${{ needs.validation-plan.result }}
          FRONTEND_EXPECTED: ${{ needs.change-scope.outputs.frontend_changed }}
          MACOS_EXPECTED: ${{ needs.change-scope.outputs.macos_sensitive }}
          PERFORMANCE_SENSITIVE: ${{ needs.change-scope.outputs.performance_sensitive }}
          BUILD_EXPECTED: ${{ needs.change-scope.outputs.release_sensitive }}
          PACKAGE_EXPECTED: ${{ needs.change-scope.outputs.full_validation }}
          PACKAGE_SMOKE_EXPECTED: ${{ needs.change-scope.outputs.package_sensitive == 'true' && needs.change-scope.outputs.full_validation != 'true' }}
          AUDIT_EXPECTED: ${{ needs.change-scope.outputs.dependency_sensitive }}
          FRONTEND: ${{ needs.frontend-quality.result }}
          MACOS: ${{ needs.rust-macos.result }}
          PERFORMANCE: ${{ needs.performance-profile.result }}
          BUILD: ${{ needs.build-macos.result }}
          MACOS_PERFORMANCE: ${{ needs.performance-macos.result }}
          PACKAGE: ${{ needs.package-macos.result }}
          PACKAGE_SMOKE: ${{ needs.package-smoke.result }}
          AUDIT: ${{ needs.dependency-audit.result }}
        run: |
          set -euo pipefail
          test "$CLASSIFIER" = success
          test "$VALIDATION_PLAN" = success
          check_expected() {
            expected="$1"
            actual="$2"
            label="$3"
            if [ "$expected" = true ]; then
              test "$actual" = success || { echo "$label expected success, got $actual"; exit 1; }
            else
              test "$actual" = skipped || { echo "$label expected skipped, got $actual"; exit 1; }
            fi
          }
          check_expected "$FRONTEND_EXPECTED" "$FRONTEND" frontend
          check_expected "$MACOS_EXPECTED" "$MACOS" rust-macos
          check_expected "$PERFORMANCE_SENSITIVE" "$MACOS_PERFORMANCE" native-macos-performance
          check_expected true "$PERFORMANCE" performance-profile
          check_expected "$BUILD_EXPECTED" "$BUILD" release-compile-macos
          check_expected "$PACKAGE_EXPECTED" "$PACKAGE" package-macos
          check_expected "$PACKAGE_SMOKE_EXPECTED" "$PACKAGE_SMOKE" package-smoke
          check_expected "$AUDIT_EXPECTED" "$AUDIT" dependency-audit
