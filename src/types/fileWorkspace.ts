// W1-01 Contract Spine seed.
//
// Codex should finalize this file against the merged W0 contracts and the
// Rust wire shapes in `src-tauri/src/file_workspace/contracts.rs`.
// Keep this module contract-only: no stores, Tauri calls, path resolution,
// byte reads, query behavior, preview runtime or filesystem mutation.

export {};
