# Codex start instructions for W1-01

Work only on branch `feat/w1-01-file-workspace-contract-spine`.

Start by reading:

1. `docs/project/tasks/W1-01-CONTRACT-SPINE-CODEX.md`
2. `docs/project/tasks/W1-01-REVIEW-CHECKLIST.md`
3. `docs/project/initiatives/W1-file-library-foundation.md`
4. the merged W0 spec set under `docs/project/specs/file-library-preview/`

Then inspect the existing seed in `src-tauri/src/file_workspace/` and finish W1-01 exactly as scoped.

Do not start W1-02 or later Tracks. Do not create schema/dependency changes, Tauri commands, stores, filesystem behavior, preview providers/UI, scheduler runtime, watcher changes or a second content-read authority.

Before declaring completion, run all available validation listed in the implementation brief, summarize changed files and validation evidence, and leave the branch ready for independent architecture/code review.
